node_modules/
*.log

# 运行时数据:含订阅凭据和 API Key,绝不入库
config.json
usage.json
last-node.txt
mihomo-zen.yaml
mihomo-data/
data/
.env
.env.*
# 例外:模板里没有真凭据,而用户就是靠它起步的(cp .env.example .env)
!.env.example

# 内核二进制体积大,由构建时下载
bin/
resources/mihomo/

# scripts/shot.mjs 的截图产物
.shots/

# 设计稿参考图和提取脚本:一次性产物,不是交付内容
.refs/
scripts/extract-imgs.mjs
